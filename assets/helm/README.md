# Connecting Systems CodeJam - Helm chart

A simple [helm](https://helm.sh/) chart to deploy the services in a Kubernetes cluster.

```bash
# Install
helm install connecting-systems-codejam .

# Uninstall
helm uninstall connecting-systems-codejam
```
